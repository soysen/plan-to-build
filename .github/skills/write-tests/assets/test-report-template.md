# {專案名稱} 測試報告（Test Report）

**版本**：v{n}  
**執行日期**：{YYYY-MM-DD HH:mm}  
**執行者**：{執行者}  
**對應測試計畫**：`docs/test/test-plan-{YYYY-MM-DD}.md`  
**環境**：local / CI / staging  
**Commit**：`{git-commit-hash}`

---

## 執行摘要

| 指標         | 數值 | 目標 |  達標   |
| ------------ | :--: | :--: | :-----: |
| 測試案例總數 | {n}  |  -   |    -    |
| ✅ 通過      | {n}  |  -   |    -    |
| ❌ 失敗      | {n}  |  0   | {✅/❌} |
| ⏭ 跳過      | {n}  |  -   |    -    |
| 整體通過率   | {n}% | 100% | {✅/❌} |
| 測試執行時間 | {n}s |  -   |    -    |

---

## 覆蓋率摘要

| 模組       | 行覆蓋率 | 分支覆蓋率 | 函式覆蓋率 | 目標  |  達標   |
| ---------- | :------: | :--------: | :--------: | :---: | :-----: |
| {模組名稱} |   {n}%   |    {n}%    |    {n}%    | ≥ 90% | {✅/❌} |
| {模組名稱} |   {n}%   |    {n}%    |    {n}%    | ≥ 80% | {✅/❌} |
| **整體**   | **{n}%** |  **{n}%**  |  **{n}%**  |   -   |    -    |

> 覆蓋率詳細報告：`coverage/index.html`（執行 `pnpm test:coverage` 後產生）

---

## 測試結果明細

### Unit Tests

| TC ID   | 測試描述   | 檔案                                    | 結果 |  耗時 | 備註       |
| ------- | ---------- | --------------------------------------- | :--: | ----: | ---------- |
| TC-U001 | {測試描述} | `src/{module}/{module}.service.test.ts` |  ✅  | {n}ms |            |
| TC-U002 | {測試描述} | `src/{module}/{module}.service.test.ts` |  ✅  | {n}ms |            |
| TC-U003 | {測試描述} | `src/{module}/{module}.service.test.ts` |  ❌  | {n}ms | 見失敗分析 |

**統計**：通過 {n} / 失敗 {n} / 跳過 {n}

---

### Integration Tests

| TC ID   | 測試描述   | 端點                      | 結果 |  耗時 | 備註 |
| ------- | ---------- | ------------------------- | :--: | ----: | ---- |
| TC-I001 | {測試描述} | `POST /api/v1/{endpoint}` |  ✅  | {n}ms |      |
| TC-I002 | {測試描述} | `GET /api/v1/{endpoint}`  |  ✅  | {n}ms |      |

**統計**：通過 {n} / 失敗 {n} / 跳過 {n}

---

### Component Tests

| TC ID   | 測試描述   | 元件              | 結果 |  耗時 | 備註 |
| ------- | ---------- | ----------------- | :--: | ----: | ---- |
| TC-C001 | {測試描述} | `{ComponentName}` |  ✅  | {n}ms |      |

**統計**：通過 {n} / 失敗 {n} / 跳過 {n}

---

### E2E Tests

| TC ID   | 使用者旅程 | 瀏覽器 | 結果 | 耗時 | 備註 |
| ------- | ---------- | ------ | :--: | ---: | ---- |
| TC-E001 | {旅程描述} | Chrome |  ✅  | {n}s |      |

**統計**：通過 {n} / 失敗 {n} / 跳過 {n}

---

## 失敗分析

> 若無失敗測試，此節可省略

### ❌ TC-U003：{失敗測試描述}

**檔案**：`src/{module}/{module}.service.test.ts:42`  
**錯誤訊息**：

```
Error: Expected "INVALID_CREDENTIALS" but received "UNKNOWN_ERROR"
  at AuthService.login (src/auth/auth.service.ts:35)
```

**根本原因**：{分析原因}

**修復建議**：{建議做法}

**對應 Bug Issue**：需建立 → 使用 `/create-issues build` 處理

---

## 覆蓋率未達標模組

> 若所有模組均達標，此節可省略

| 模組       | 目前覆蓋率 | 目標  | 缺口  | 未覆蓋的主要路徑     |
| ---------- | :--------: | :---: | :---: | -------------------- |
| {模組名稱} |    {n}%    | ≥ 90% | -{n}% | {未覆蓋的函式或分支} |

**建議補充的測試案例**：

- `{describe}.{it}` — 覆蓋 {未覆蓋的情境}

---

## 需建立的 Bug Issues

> 以下失敗測試需建立 Bug Issue 追蹤

| TC ID   | 問題摘要   | 嚴重度  | 建議 Labels            |
| ------- | ---------- | :-----: | ---------------------- |
| TC-U003 | {問題摘要} | 🔴 High | `bug`, `priority:high` |

執行 `/create-issues build` 可自動建立上述 Issues。

---

## 與測試計畫對比

| 總 TC 數 | 已實作 | 未實作 | 通過 | 失敗 |
| :------: | :----: | :----: | :--: | :--: |
|   {n}    |  {n}   |  {n}   | {n}  | {n}  |

**未實作的測試案例**（仍為 `// TODO`）：

- TC-XXX：{描述}

---

## 結論與建議

**本次測試結論**：{通過 / 部分通過 / 未通過}

**後續行動**：

- [ ] 修復失敗測試（見失敗分析）
- [ ] 補充覆蓋率不足的模組
- [ ] 建立 Bug Issues（執行 `/create-issues build`）
- [ ] 重新執行測試確認修復

---

## 修訂記錄

| 版本 | 日期   | 執行者 | 說明     |
| ---- | ------ | ------ | -------- |
| v1   | {日期} | {作者} | 初次執行 |
