# Agent Entry Point

這個檔案是給 Antigravity 與其他支援 `AGENTS.md` 的 agent 使用的快捷入口。

本 workspace 的正式規範與技能定義集中在 .github，請優先讀取以下檔案：

1. `.github/copilot-instructions.md`
2. `.github/skills/using-agent-skills/SKILL.md`
3. `.github/skills/` 底下與目前任務對應的 `SKILL.md`

建議流程：

1. 先依 `.github/copilot-instructions.md` 的決策樹選擇 skill。
2. 讀取對應 `.github/skills/<skill-name>/SKILL.md`。
3. 按 skill 步驟執行，不要自行發明流程。

請不要把規範複製到這個檔案；這裡只負責導向 .github。
